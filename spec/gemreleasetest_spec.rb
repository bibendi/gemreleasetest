require "spec_helper"

describe Gemreleasetest do
  it "has a version number" do
    expect(Gemreleasetest::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
